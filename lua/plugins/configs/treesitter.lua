local options = {
  ensure_installed = { "lua", "html", "css", "javascript", "typescript", "json" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
  autotag = {
    enable = true,
  },
}

return options
